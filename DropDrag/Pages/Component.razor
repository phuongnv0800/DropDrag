@using DropDrag.Data

<div class="component-block d-flex">
    <span>
        .
    </span>
    <div class="d-flex align-items-center">
        @if (item != null)
        {
            <div>
                <p class="title">@item.Title</p>
                <p class="p-12">@item.Description</p>
            </div>
        }
    </div>
</div>

@code {
    [Parameter] public TodoItem item { get; set; }
}
